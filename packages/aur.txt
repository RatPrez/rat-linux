# AUR packages installed unconditionally via yay. One per line.
# Anything user-optional lives in packages/categories/*.toml instead.

appimagelauncher      # integrates .AppImage files into the app menu
imageviewer           # lightweight image viewer
vesktop               # Discord client with Vencord built in, native Wayland screenshare
