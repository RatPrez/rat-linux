# Official-repo packages installed via pacman.
# One per line. Blank lines and # comments ignored.

# --- GPU drivers ---
# Handled separately by install/05-gpu-drivers.sh based on detected hardware:
# packages/gpu-{amd,intel}.txt and packages/gpu-nvidia-{open,proprietary}.txt.

# --- Filesystems (Windows drive support) ---
ntfs-3g             # read/write NTFS
exfatprogs          # mkfs/fsck for exFAT
dosfstools          # mkfs/fsck for FAT/FAT32
mtools              # access FAT filesystems without mounting (MS-DOS tools)
udisks2             # disk mounting service (backs GNOME Disks + Dolphin's device pane)
polkit              # auth backend udisks2 needs for user-triggered mount/format

# --- KDE Plasma (Wayland) ---
plasma-meta
sddm
sddm-kcm            # System Settings module to theme/configure the SDDM login
xorg-xwayland
xdg-desktop-portal
xdg-desktop-portal-kde
bluedevil
power-profiles-daemon

# --- Audio (PipeWire) ---
pipewire
pipewire-pulse
pipewire-alsa
wireplumber
sof-firmware        # Intel SOF codecs (most laptops) — no devices without this
alsa-ucm-conf       # per-machine UCM profiles so devices actually surface
alsa-firmware       # extra codec firmware blobs
alsa-utils          # alsamixer / speaker-test / aplay for debugging

# --- Clipboard ---
wl-clipboard        # wl-copy / wl-paste for Wayland clipboard
xclip               # clipboard access for XWayland/X11 apps

# --- Networking / Bluetooth ---
networkmanager
bluez
bluez-utils

# --- Gaming / Vulkan ---
steam
vulkan-icd-loader
lib32-vulkan-icd-loader
gamemode
lib32-gamemode
wine
winetricks

# --- Apps ---
alacritty
blender
obs-studio
mpv
proton-vpn-gtk-app
qbittorrent
spotify-launcher
zed                 # code editor (default for text/code)
dolphin             # KDE file manager
elisa               # KDE music player (default audio)
kate                # advanced text editor — also provides the `kwrite` binary
kcalc               # KDE calculator
spectacle           # KDE screenshot tool
ark                 # KDE archive manager
libreoffice-fresh   # office suite
gnome-disk-utility  # GNOME Disks — partition / mount / SMART GUI

# --- CLI / system tools ---
btop                # process + resource monitor
fastfetch           # system info
cpupower            # CPU governor control (cpupower.service, see README)
nano
xdotool
wmctrl
cpu-x               # GUI system/CPU info (neofetch-ish but graphical)
webkit2gtk-4.1       # embedded web renderer some apps depend on

# --- Codecs ---
gst-libav           # GStreamer FFmpeg-backed codecs
gst-plugins-bad
gst-plugins-ugly

# --- Dev toolchains ---
dotnet-sdk
dotnet-sdk-8.0
bun
mingw-w64-binutils   # Windows cross-compile toolchain
mingw-w64-crt
mingw-w64-gcc
mingw-w64-headers
mingw-w64-winpthreads
python-pip
python-pipx
python-pyqt6

# --- Secrets ---
# ProtonVPN (and other apps) store credentials via the Secret Service API.
# gnome-keyring provides that backend; 10-postinstall resets a stale/locked
# login keyring so ProtonVPN can save its session on a fresh install.
gnome-keyring

# --- Fonts ---
noto-fonts
noto-fonts-cjk
noto-fonts-emoji
noto-fonts-extra
ttf-jetbrains-mono-nerd   # terminal / editor font (now in [extra], not the AUR)

# --- Flatpak ---
flatpak
